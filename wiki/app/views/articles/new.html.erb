<h1>Create New Article</h1>

<% if @article.errors.any? %>
  <% @article.errors.full_messages.each do |error_msg| %>
    <p id="error"><%= error_msg %></p>
  <% end %>
<% end %>

<%= form_for @article do |article| %>
  <p>
    <%= article.label :title %>
    <%= article.text_field :title %>
  </p>
  <%= article.fields_for :updates do |update| %>
    <p>
      <%= update.label :user_id %>
      <%= update.number_field :user_id %>
    </p>
    <p>
      <%= update.label :content %>
      <%= update.text_area :content%>
    </p>
  <% end %>
  <%= article.submit %>
<% end %>
